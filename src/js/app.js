import React from 'react' // eslint-disable-line no-unused-vars
import ReactDOM from 'react-dom'
import Main from './workoutComponent/main'

ReactDOM.render(
  <section>
    <Main />
  </section>
  ,
  document.querySelector('main')
)